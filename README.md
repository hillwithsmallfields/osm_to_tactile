# osm_to_tactile: make 2.5D data from openstreetmap

This software fetches street data from openstreetmap.org and generates laser cutter data.

As streets normally form a connected network, the result should be a
single sheet with cutouts, that can be stuck onto a background to make
a tactile map.
